import React, {useState} from 'react'; import axios from 'axios'; export default function AIChat(){ const [prompt,setPrompt]=useState(''); const [messages,setMessages]=useState([]); const send=async()=>{ if(!prompt) return; const res = await axios.post('/api/ai/chat',{ prompt }); setMessages(m=>[...m,{ sender:'user', text:prompt },{ sender:'ai', text:res.data.output }]); setPrompt(''); } return (<div className='container p-6'><h2>AI Chat</h2><div className='border p-4 h-64 overflow-auto'>{messages.map((m,i)=>(<p key={i}><strong>{m.sender}:</strong> {m.text}</p>))}</div><div className='mt-4'><input value={prompt} onChange={e=>setPrompt(e.target.value)} className='border p-2 w-3/4' placeholder='Ask anything...'/><button onClick={send} className='ml-2 bg-indigo-600 text-white px-4 py-2 rounded'>Send</button></div></div>); }