import React, {useEffect, useState} from 'react'; export default function Voice(){ const [listening,setListening]=useState(false); useEffect(()=>{},[]); const start=()=>{ alert('This demo uses Web Speech API in browser'); }; return (<div className='container p-6'><h2>Voice Chat</h2><p>Start voice chat (demo)</p><button onClick={start} className='bg-blue-500 text-white p-2 rounded'>Start Voice</button></div>); }